# Bundled into both Lambda deployment packages (zip or container image).
# Only PyJWT (with its crypto backend) is needed beyond the stdlib + boto3,
# and boto3 is already present in the Lambda runtime.
PyJWT==2.9.0
cryptography==43.0.1
